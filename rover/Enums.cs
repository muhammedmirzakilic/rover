using System;
namespace rover
{
    public enum Instruction : byte
    {
        L,
        R,
        M
    }

    public enum Direction : byte
    {
        S,
        E,
        N,
        W
    }
}
